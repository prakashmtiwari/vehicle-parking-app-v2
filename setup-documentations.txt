1. Docker commands used

# build image 
docker build -t image_name -f Dockerfile_name .

# Run container
docker run --net='host' image_name


docker compose config

docker compose up --watch

docker compose up --watch --build --force-recreate



Kubernetes

push the docker images created above to docker hub

use the reference in deployment configuration.


For service of type nodeport in kind or minikube
kubectl port-forward svc/frontend-service 8080:80


